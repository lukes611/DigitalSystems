INCLUDE "EIGHTBITREGISTER";

SUBDESIGN MOD130COUNTER(
RESET, CLK :INPUT;
OUTNUM[7..0] :OUTPUT;
)
VARIABLE
co : EIGHTBITREGISTER;
BEGIN
co.CLK = CLK;
IF co.OUTN[7..0] == 130 OR RESET THEN
	co.CLR = VCC;
ELSE
	co.CLR = GND;
END IF;

OUTNUM[] = co.OUTN[7..0];
END;